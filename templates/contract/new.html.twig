{% extends 'base.html.twig' %}

{% block title %}{{ 'newContract'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'newContract'|trans }}</h1>

    {{ include('contract/_form.html.twig') }}

    <a href="{{ path('course_index') }}">back to list</a>
{% endblock %}
