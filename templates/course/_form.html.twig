{{ form_start(form) }}
    <div class="row">
        <div class="offset-md-3 col-md-6">
            <div class="card">
                <div class="card-body">
                    {{ form_widget(form) }}
                    <button class="btn btn-success">{{ 'saveForm'|trans }}</button>
                </div>
            </div>
        </div>
    </div>
{{ form_end(form) }}
